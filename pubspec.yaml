name: sw_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.6

  # --- 지도 패키지 ---
  google_maps_flutter: ^2.7.0
  url_launcher: ^6.3.2
  geolocator: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # --- 커스텀 폰트 (주석 처리) ---
  # fonts:
  #   - family: ChangwonDangamAsac
  #     fonts:
  #       - asset: assets/fonts/ChangwonDangamAsac-Bold.ttf
  #         weight: 700